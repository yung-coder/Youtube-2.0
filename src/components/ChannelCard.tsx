import React from "react";

type ChannelCardProps = {
  items: any;
};

const ChannelCard: React.FC<ChannelCardProps> = ({items}) => {
  return <div>Have a good cahnndel</div>;
};
export default ChannelCard;
